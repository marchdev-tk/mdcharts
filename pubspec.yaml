name: mdcharts
version: 6.3.6
homepage: https://github.com/marchdev-tk/mdcharts
repository: https://github.com/marchdev-tk/mdcharts
issue_tracker: https://github.com/marchdev-tk/mdcharts/issues
description: march.dev charts library. Provides highly customizable and configurable charts.

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  
  # common
  meta: ^1.15.0
  rxdart: ^0.28.0
  decimal: ^3.0.2
  intl: ^0.19.0

  # marchdev.tk
  cross_platform: ^3.0.1
  flinq: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
